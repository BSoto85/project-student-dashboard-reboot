import React from "react";

const Aside = ({ students }) => {
  
//   const cohortList = students.map(student => {
//   return   student.cohort.cohortCode
//  });

//  const deletedDuplicatesCohortList

  return (<div>
  
  </div>)
};

export default Aside;
